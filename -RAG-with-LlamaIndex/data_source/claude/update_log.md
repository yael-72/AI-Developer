# יומן עדכונים - סוכן Claude
**תאריך:** מרץ 2026

### שינויים ב-DB:
* בוצע שינוי במבנה ה-DB: נוספה טבלת `users_preferences` לניהול הגדרות אישיות.
* הוסרו שדות ישנים מטבלת `products` (שדות `old_price`).

### מגבלות טכניות:
* קיימת מגבלה בחיבור ל-API החיצוני - הוא מאפשר עד 100 קריאות בדקה בלבד.
* רכיב ה-Authentication הוגדר כבעייתי ורגיש במיוחד, אין לשנות בו לוגיקה ללא בדיקות יחידה מקיפות.